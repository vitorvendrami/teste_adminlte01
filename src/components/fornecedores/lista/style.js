import { StyleSheet, Dimensions } from 'react-native'

const Styles = StyleSheet.create({
  mainview: {
    backgroundColor: '#FFF',
    width:400,
    borderWidth: 1,
    borderColor: '#337ff2',
    height: 50,
    flexDirection: 'row',
    alignItems: 'center',
  },
  text:{
    fontSize: 18,
  }
})

export default Styles